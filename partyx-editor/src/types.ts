export type EditorConfig = {
  emissionRate: number; // partículas/seg
  lifetime: number; // seg
  speed: number; // px/seg
  size: number; // px
  spread: number; // radianes
  tint: string; // "#RRGGBB"
};
